# C2Sherlock AI分析报告

## 一. 执行摘要

综合判定：恶意 C2 通信（高危，结论置信度：高）。源主机 192.168.56.5 通过 HTTP 协议与目标 192.168.56.4:80 进行通信，共发起 15 次 HTTP 请求。分析发现，HTTP 请求中的 Host 字段设置为 www.amazon.com，但实际连接目标为内网 IP 192.168.56.4，存在服务身份与真实目标不一致的异常；同时，时序检测模型识别出自动化通信特征，最高风险评分达 100.0%（Critical），表明通信节奏符合程序化 Beacon 行为。综合上述证据，判定该通信为恶意 C2 通信，建议优先限制该通信并核查源主机。

## 二. 关键通信特征

**通信画像**

| 项目 | 观测结果 | 说明 |
|------|----------|------|
| 源主机 | 192.168.56.5 | 发起通信的内部主机 |
| 主要目标 | 192.168.56.4:80 | 通信对端，内网 IP 地址 |
| 协议与服务 | HTTP | 使用 HTTP 协议，未启用 TLS |
| HTTP 请求数 | 15 | 样本中捕获的 HTTP 请求数量 |
| TLS 会话数 | 0 | 未检测到加密会话 |

**关键证据**

| 发现 | 样本证据 | 安全含义 |
|------|----------|----------|
| 服务身份与真实目标不一致 | HTTP Host 字段为 `www.amazon.com`，但实际连接目标为 `192.168.56.4:80` | 可能用于掩饰真实控制对象或通信用途，规避基于域名的检测 |
| 自动化时序通信特征 | 时序检测命中 1 个通信组，最高模型风险评分 100.0%（Critical） | 通信节奏或数据模式更接近程序化 Beacon，需结合其他证据核查 |

## 三. 风险评估

**风险理由**：样本中 HTTP 通信的服务标识与真实目标不一致，且通信时序呈现自动化特征，模型评分达到 Critical 级别，表明该通信具有明显的恶意 C2 行为特征。

**潜在影响**：若确认为 C2 通信，攻击者可能通过该通道下发指令、窃取数据或进行横向移动，对内部网络安全构成严重威胁。

**结论边界**：RITA 检测给出 Beacon Score 0.518，风险判定为低风险，但时序检测模型给出了高风险评分，两者存在差异。本报告基于模型检测结果和异常证据作出判定，但最终确认需结合终端取证等主机层面证据。

## 四. 处置建议

1. 限制源主机 192.168.56.5 与目标 192.168.56.4:80 之间的通信，并保全本次流量和相关日志。
2. 核查源主机 192.168.56.5 上发起该通信的进程、父子进程、计划任务、启动项及近期新增文件。
3. 在网关、DNS、代理和终端检测平台中检索相同目标、Host 字段（www.amazon.com）、URI、User-Agent 或相似通信节奏的流量。
4. 结合终端取证确认是否存在命令执行、文件传输、凭据访问或横向移动等后续行为。

## 五. 溯源归因

### 行为研判结论

样本中源主机 192.168.56.5 向目标 192.168.56.4:80 发起 15 次 HTTP 请求，请求的 Host 字段伪造为 www.amazon.com，实际连接内网 IP，且通信时序呈现自动化 Beacon 特征。该行为符合恶意 C2 通信的典型模式，可能用于命令控制或数据外泄。

### 行为归因说明

| ATT&CK 技术 | 中文名称 | 通俗含义 | 本样本证据 |
|-------------|----------|----------|------------|
| T1071.001 | 应用层协议：Web 协议 | 攻击者使用 HTTP/HTTPS 等 Web 协议进行 C2 通信，以融入正常流量 | 样本使用 HTTP 协议进行 15 次请求，符合 Web 协议通信特征 |
| T1036 | 伪装 | 攻击者通过伪装成合法服务或文件以规避检测 | HTTP Host 字段设置为 www.amazon.com，但实际连接内网 IP，属于服务标识伪装 |

### 组织归因说明

候选组织关联（辅助研判，非确认结论）

| 候选组织 | 画像关联度 | 中文背景说明 | 重合行为 |
|----------|------------|--------------|----------|
| APT42 | 53.0% | APT42 是一个由伊朗支持的威胁组织，从事网络间谍活动和监视，主要关注中东地区，使用鱼叉式网络钓鱼和恶意软件，通过 Web 协议进行 C2 通信。 | Web 协议 |
| APT39 | 51.4% | APT39 是伊朗情报与安全部（MOIS）使用的网络间谍活动名称，主要针对旅行、酒店、学术和电信行业，使用 Web 协议进行通信。 | Web 协议 |
| APT18 | 50.7% | APT18 是一个自 2009 年以来活动的威胁组织，目标涵盖科技、制造、人权组织等，使用 Web 协议进行 C2。 | Web 协议 |

以上候选组织关联度仅反映行为相似程度，不能作为确认攻击组织身份的依据。