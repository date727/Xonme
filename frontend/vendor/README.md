# Frontend vendor libraries

`index.html` currently loads ECharts and Marked from a version-pinned CDN.
Before an offline demonstration or competition delivery, download the matching
minified files into this directory and replace the CDN script URLs with these
local files.
